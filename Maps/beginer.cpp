#include<bits/stdc++.h>
using namespace std;

int main(){
    int arr[] = {3,1,4,6,2,5};
    map<int,int> prac;
    int n = sizeof(arr)/sizeof(arr[0]);
    
    for(int i=0;i<n;i++){
        prac.insert(i,arr[i]);
    }

    sort(arr,arr+n);
    for(int i =0;i<n;i++){
        
    }

    
}